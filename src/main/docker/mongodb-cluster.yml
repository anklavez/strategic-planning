version: '2'
services:
    strategicplanning-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb strategicplanning-mongodb-config
    strategicplanning-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    strategicplanning-mongodb-config:
        image: mongo:3.6.3
        container_name: strategicplanning-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
